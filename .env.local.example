# Copy this file to .env.local and fill in values

# Better Auth
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-here
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# Database
# Local Docker (after running: docker compose up -d):
DATABASE_URL=postgresql://kanban:kanban@localhost:5432/kanban

# Production Neon (replace with your actual connection string):
# DATABASE_URL=postgresql://<user>:<password>@ep-<name>.neon.tech/<dbname>?sslmode=require
